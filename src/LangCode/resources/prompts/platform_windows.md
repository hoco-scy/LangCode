# Windows 平台信息

- 操作系统: Windows
- Shell: PowerShell（使用 PowerShell 语法，如 `$null`, `$env:VAR`, 反引号换行）
- 路径分隔符: `\`（也可用 `/`）
- Python: 使用 `python` 或 `py` 命令
- 常用命令: `Get-ChildItem` (ls), `Set-Location` (cd), `Get-Content` (cat), `Remove-Item` (rm)
